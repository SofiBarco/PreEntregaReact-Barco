import React, {useState, useEffect} from 'react';

const WeatherApp = () => {
    
    const [clima, setClima] = useState([]);

    useEffect(() => {
    
        fetch () 
    }, []);
    
    
    return (
        <>
            
        </>
    );
}

export default WeatherApp;
